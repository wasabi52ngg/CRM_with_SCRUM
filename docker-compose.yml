services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: itcrm
      POSTGRES_USER: itcrm
      POSTGRES_PASSWORD: itcrm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
